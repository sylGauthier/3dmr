#include <game/scene/node.h>
#include "context.h"

#ifndef GAME_OPENGEX_CAMERA_NODE_H
#define GAME_OPENGEX_CAMERA_NODE_H

int ogex_parse_camera_node(const struct OgexContext* context, struct Node* node, const struct ODDLStructure* cur);

#endif
